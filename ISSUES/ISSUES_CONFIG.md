# イシュー: 設定ファイルおよび環境変数のロード機能の不足

## 概要
`lasada` は `config-rs` を用いて、`config.toml` および `~/.config/lasada/config` から設定を読み込み、環境変数 (`LLM_API_KEY`) とオーバーライドする機能を備えているが、`pakila` では一切未実装。

## 詳細タスク
- [x] `src/Config/Loader.lean`: TOML 形式の設定ファイルをパースし、アプリケーション設定構造体へマッピングする機能。
- [x] システム環境変数と設定ファイルの値の優先順位に基づく、設定値の統合管理機能。
