# イシュー: ログローテーションと詳細設定の不足

## 概要
`lasada` は `fern` ライブラリにより、起動ごとの時刻ベースのファイルへのログローテーションを自動で行っている。`pakila` では単純な標準出力以外が実装されていない。

## 詳細タスク
- [x] `src/Observability/LogRotation.lean`: 起動時刻に基づいたログファイル名の生成と、ファイルへの追記を行う排他制御ロジック。
- [x] `main.rs` にあるレベル指定（--debug）と連動した、ログレベルの動的切り替えの実装。
