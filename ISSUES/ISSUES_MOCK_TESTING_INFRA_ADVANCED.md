# イシュー: MockLlm を用いた統合テスト基盤の拡充

## 概要
`lasada` のテストコードでは `MockLlm` を用いて「特定の入力に対して特定のツールコールを生成させる」といった詳細なテストが行われている。`pakila` ではモックの実装はスタブにとどまっている。

## 詳細タスク
- [x] `test/Mock/LlmMock.lean`: 履歴内のコンテンツを解析し、確定したツールコール（Bashコード等）を返す `MockLlm` のロジックの実装。
- [x] テスト実行環境における、インタプリタループのステップごとの状態検証スクリプトの整備。
