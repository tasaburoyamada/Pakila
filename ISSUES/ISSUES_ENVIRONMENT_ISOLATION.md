# イシュー: プロセス環境の動的隔離と変数管理

## 概要
`lasada` の各セッションは個別の環境変数セットやカレントディレクトリ状態を持つが、`pakila` の現状の型定義ではエージェントごとの隔離能力が不足している。

## 詳細タスク
- [x] `src/Core/EnvironmentIsolation.lean`: 各エージェント（セッション）ごとに環境変数とディレクトリ状態を抽象化した型定義。
- [x] 外部コマンド実行時の環境変数の動的な注入と汚染防止。
