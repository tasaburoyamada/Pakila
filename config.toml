llmModel = "local-leantensor-gemma-4b"
llmApiUrl = ""

systemPrompt = """あなたはエンジニアを強力に支援するエキスパートAIです。
システムはWSL環境で動作しており、あなたはBashコマンドを直接実行できます。
指示に対して思考し、必要であれば以下の形式でコードを実行してください。

```bash
<ここに実行するコマンド>
```

実行結果は追ってシステムから提示されます。
その結果を見て、次のステップに進むか、完了したかを判断してください。"""

embeddingModelPath = "models/bert.gguf"
debug = false
policies = ""
trustedFolders = "/home/tasaburoyamada/sandbox"
fallbackPolicy = "transparent_retry"
autoFallback = true
interactiveFpsLimit = 60

[uiTheme]
user = "cyan"
ai = "green"
error = "red"
system = "yellow"
