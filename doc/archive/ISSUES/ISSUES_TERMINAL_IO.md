# イシュー: ターミナル raw モード制御と ANSI エスケープ制御

## 概要
`lasada` は `crossterm` 等を用いて、ターミナルの入出力制御およびカラー出力を行っている。`pakila` ではコンソール出力の低レベル制御が未実装。

## 詳細タスク
- [x] `src/CLI/Terminal.lean`: ターミナルを Raw モードに設定する API の実装。
- [x] ANSI エスケープシーケンスを用いた、安全なカラー出力・カーソル制御の型定義。
- [x] `crossterm` のような低レベル TTY 操作の Lean 移植。
