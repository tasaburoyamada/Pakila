# イシュー: 証明義務の未充足と形式的検証の不足

## 概要
現在の `MainLoop` やプラグインには、ロードマップで定義した「不変条件」や「安全制約」の証明が不足しており、仮の実装にとどまっている。

## 詳細タスク
- [x] `history_monotone` 以外の安全不変条件（状態が負にならないこと、LLM が必ず assistant ロールを返すこと等）の定理構築。
- [x] ループ停止性定理 (`loop_terminates`) の完全な記述と Lean カーネルでの検証。
- [x] べき等性テスト（`BashEngine`）の形式的な証明（実行結果が等価であることの証明）。
